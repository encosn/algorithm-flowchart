# algorithm-flowchart — 문제 정의와 순서도 그리기

> 한 줄 소개: **문제 상황을 보고 해결해야 할 문제를 정의하고, 순서도로 알고리즘을 그린 다음, 그 순서도를 의사코드로 옮겨 보는 3단계 실습 앱**

## ① 목적과 대상 단원

- **단원**: 중학교 정보 3단원 **알고리즘과 프로그래밍**
- **성취기준**
  | 코드 | 내용 | 앱에서 다루는 곳 |
  |:---|:---|:---|
  | [9정03-01] | 문제의 상태를 정의하고 수행 가능한 형태로 구조화한다 | 1단계 문제 정의 (초기 상태·목표 상태) |
  | [9정03-02] | 문제 해결을 위한 추상화의 중요성을 이해하고, 핵심요소를 중심으로 알고리즘을 표현한다 | 2단계 순서도 그리기 |
  | [9정03-04] | 사례를 중심으로 문제 해결에 적합한 전략을 선택하여 알고리즘을 설계한다 | 3단계 알고리즘 설계(의사코드) |
- **[비상교육] 중학_정보_3_교사용 교과서** III단원 "1. 추상화와 알고리즘"(순서도 기호·순차/반복/선택 구조,
  자연어·순서도·의사코드라는 세 가지 표현 방법)과 "2. 프로그래밍" 프로젝트 실습(문제 상황 → 해결해야 할
  문제 → 문제 분해 → 핵심요소 추출 → 알고리즘 설계)의 흐름을 참고해 만들었다. 문제 상황 4개(자동판매기,
  쓰레기 줍기 게임, 도서관 연체, 놀이공원)는 교과서 예시를 각색한 것이고 원문을 그대로 베끼지 않았다.
- **문제 분해·핵심요소 추출 단계는 일부러 넣지 않았다.** 사용자가 "문제 정의 + 순서도"까지만 먼저 요청했고,
  이후 "3단계 알고리즘 설계"를 추가로 요청해서 지금의 3단계 구성이 됐다.
- **3단계는 순서도를 자동으로 "번역"해 주지 않는다.** 어떤 기호가 몇 번째로 오는지, 판단 기호의 두 갈래가
  어디서 다시 만나거나 반복되는지는 `algorithm-core.js`가 그래프를 따라가며 자동으로 알아내 순서를
  잡아 주지만, **각 기호에서 무엇을 할지 의사코드 문장으로 쓰는 것은 학생이 직접 한다.** 번역 자체를
  자동화하면 "핵심요소를 중심으로 알고리즘을 표현한다"는 성취기준([9정03-02])의 학습 목표가 사라지기
  때문이다. 순차/반복/선택 구조를 알아보는 것(교과서 "3 알고리즘의 구조")도 학생이 직접 겪어야 할 부분이라,
  판단 기호의 두 갈래를 "그렇다면(예)/아니면(아니요)"로 자동 표시는 해 주지만 그 조건과 내용을 어떻게
  쓸지는 비워 둔다.

## ② 기능 목록

| 단계 | 기능 |
|:---|:---|
| ① 문제 정의 | **학번·이름** 입력칸 (PDF 머리말에만 쓰이고 **저장하지 않는다** — 아래 "학번·이름" 절 참고) |
| | 문제 상황 카드 4개(자동판매기·쓰레기 줍기 게임·도서관 연체·놀이공원) + 직접 입력하기 |
| | 해결해야 할 문제 / 초기 상태 / 목표 상태 입력란, 항목마다 힌트 보기 버튼 |
| | **다 안 쓰면 2·3단계로 못 넘어간다.** 빈 칸이 있는 채로 넘어가려 하면 이동하지 않고, 「무엇이 비었는지」 목록으로 알려 주며 그 칸을 빨갛게 표시하고 첫 빈 칸으로 커서를 옮긴다. 칸을 채우는 즉시 경고가 줄어들고, 다 채우면 사라진다 (아래 "1단계를 다 써야 넘어간다" 절 참고) |
| ② 순서도 그리기 | 팔레트에서 기호 4종(시작/끝·입출력·처리·판단)을 눌러 캔버스에 배치 (배치 모드는 **연속 배치되게 sticky**하다 — 계속 클릭하면 여러 개, 팔레트 버튼을 다시 눌러야 해제) |
| | 기호를 누르면(클릭, 드래그 아님) **기호 위에 바로 입력창이 뜬다** — 별도 대화상자 없이 그 자리에서 타이핑. `Enter`로 저장, `Esc`로 취소, 다른 곳을 누르면(블러) 자동 저장. 새로 놓은 기호는 놓자마자 입력창이 자동으로 열린다 |
| | 기호를 드래그하면 이동 (이동 임계값 3px — 그 이하 움직임은 클릭=편집으로 처리). **배치 모드에서도 드래그로 옮길 수 있다** — 이미 놓은 기호를 누르면 새로 놓지 않고 그 기호를 편집/이동한다 |
| | 연결하기 모드: 두 기호를 순서대로 눌러 화살표 연결. 판단 기호는 첫 화살표에서 `confirm()`으로 예/아니요를 물어보고, 두 번째 화살표는 남은 라벨을 자동으로 붙인다 (세 번째 시도는 `alert()`로 막음) |
| | 삭제하기 모드: 기호나 화살표를 눌러서 삭제 |
| | **↶ 되돌리기 / ↷ 다시 실행** — 기호 놓기(+놓자마자 입력한 이름까지 한 단계)·이동·나중에 편집·연결·삭제·전체 지우기를 한 단계씩 되돌린다. 새로고침하면 기록이 비워진다(문제 정의 답이나 캔버스 크기는 되돌리기 대상이 아니다) |
| | **캔버스 크기 조절** — 캔버스 오른쪽 아래 모서리(⤡ 손잡이)를 드래그하면 그리는 공간 자체가 커지거나 작아진다(줌이 아니라 실제 여백 변화). 크기도 `localStorage`에 저장됨 |
| | 점검하기: `FlowchartCore.validate()` 결과를 성공(초록)/실패(빨강, error) + 경고(주황, warn) 로 보여줌 |
| | 그림으로 저장 (SVG → 2배 확대 PNG 다운로드), 전체 지우기(캔버스만, 되돌리기로 복원 가능), 처음부터 다시(전체 초기화 + 새로고침, 이것만은 되돌릴 수 없음) |
| ③ 알고리즘 설계 | 2단계 순서도가 `FlowchartCore.validate()` 를 통과해야만 열린다. 안 통과하면 "2단계로 돌아가기" 안내만 보임 |
| | `AlgorithmCore.buildOutline()` 이 순서도를 시작 기호부터 따라가며 기호 순서를 정하고, 판단 기호의 예/아니요 갈래·되돌아가는 반복·다시 만나는 지점을 자동으로 찾아 카드 목록으로 보여줌 (내용 자체는 학생이 씀 — 자세한 이유는 위 ① 참고) |
| | 카드마다 원래 순서도 기호의 내용을 참고로 보여주고, 그 아래 빈칸에 의사코드 문장을 직접 입력. 판단 기호는 "그렇다면(예)/아니면(아니요)"로 자동 표시되고 안쪽 내용은 한 단 들여써서 보여줌. 반복(↻)·다시 만남(↘) 지점은 학생이 쓸 필요 없이 자동 표시 |
| | 미리보기: 지금까지 쓴 내용을 들여쓰기가 적용된 의사코드 모양으로 실시간으로 보여줌 |
| | 점검하기: `AlgorithmCore.checkAnswers()` 로 아직 안 쓴 빈칸을 찾아 알려줌 |
| | **PDF로 저장하기** — 1·2·3단계를 한 보고서로 묶어 브라우저 인쇄 기능으로 내보낸다 (학번·이름이 비어 있으면 한 번 물어본다). **「글로 저장」(.txt)은 2026-08-04에 삭제했다** — PDF 하나로 충분하고 버튼이 많아 헷갈렸다 |
| | **🖨 PDF로 저장하기** — 문제 정의(1단계) + 순서도 그림(2단계) + 의사코드(3단계)를 한 장의 보고서로 묶어 인쇄 대화상자를 띄운다. 학생이 프린터 목록에서 "PDF로 저장"을 고르면 파일이 된다 (외부 라이브러리 없음 — 아래 "PDF로 저장" 절 참고) |
| 이동 | 2단계 아래 **"3단계: 알고리즘 설계 →"** 버튼으로 넘어간다 (1단계의 "2단계: 순서도 그리기 →" 와 같은 자리·모양). 3단계가 아직 열릴 수 없는 상태면 3단계 화면 안에서 "2단계로 돌아가기" 안내가 보인다 |
| 공통 | 1단계 답·2단계 순서도·캔버스 크기·3단계 의사코드 답을 `localStorage` (`algorithm-flowchart:v1`) 에 자동 저장 — 새로고침해도 유지, 서버 전송 없음 |

## ③ 현재 개발 상태와 다음 할 일

- **1차 완성 (2026-08-03)** · 포트 8086 · 단위 시험 93개 통과 (`npm test` = flowchart-core.js 52개 + algorithm-core.js 41개)
- **2026-08-03 오후**: 사용자 피드백으로 네 가지 수정 — ① 기호 내용 수정을 `prompt()` 대화상자에서
  **캔버스 위 바로 입력(인라인 textarea)** 으로 바꿈 ② 배치 모드(sticky)에서도 이미 놓은 기호를
  드래그로 옮기고 클릭으로 편집할 수 있게 함(이전엔 기본 모드에서만 가능했음) ③ **되돌리기/다시 실행**
  버튼 추가(순서도만 대상, 놓기+즉시 입력은 한 단계로 합침) ④ **캔버스 크기 조절**(오른쪽 아래
  모서리 드래그, 실제 그리는 공간이 커짐/작아짐). 자세한 이유는 아래 각 절 참고.
- **2026-08-03 저녁**: **3단계 "알고리즘 설계" 추가.** 새 파일 `algorithm-core.js`(그래프 순회 순수
  로직, `tests/run-algorithm-tests.js` 41개) + `main.js`의 렌더링/입력/점검 코드. 자세한 설계는
  아래 "3단계 알고리즘 설계" 절 참고.
- **2026-08-03 밤**: ① 2단계 → 3단계 **이동 버튼**(`#btn-to-algorithm`) 추가 ② 3단계에 **PDF로
  저장하기**(`#btn-algo-pdf`) 추가 — 1·2·3단계 내용을 한 보고서로 묶어 브라우저 인쇄 기능으로
  내보낸다. 아래 "PDF로 저장" 절 참고.
- **2026-08-03 밤(2)**: **1단계를 다 안 쓰면 2·3단계로 넘어가지 못하게** 막았다
  (`defineMissing()`/`renderDefineFeedback()`, 탭 이동이 전부 `goToTab()` 하나를 지나가게 정리).
  아래 "1단계를 다 써야 넘어간다" 절 참고.
- **2026-08-04**: ① 3단계 **「글로 저장」(.txt) 버튼 삭제** (PDF 하나로 충분) ② 1단계에
  **학번·이름 입력칸 추가** — PDF 머리말에만 쓰고 **저장하지 않는다**(교실 공용 컴퓨터에서
  다음 학생에게 이름이 남지 않게. 아래 "학번·이름" 절 참고).
- 실제 브라우저에서 두 시나리오 카드 선택, 문제 정의 입력, 도형 4종 배치, 연결(판단 분기 포함), 삭제,
  드래그(기본·배치 모드 둘 다), 인라인 입력창(자동 열림·Enter·Esc), 되돌리기·다시 실행(놓기+입력
  한 단계 합침·별도 편집은 별도 단계·드래그 한 단계), 캔버스 크기 조절(최소/최대 clamp·새로고침 후
  유지), 점검하기(성공/실패 둘 다), 커스텀 시나리오, 새로고침 후 복원, 전체 초기화까지 수동으로 확인했다.
- **3단계도 실제 브라우저에서 확인**: 순서도 미완성 상태에서 3단계 진입 시 안내 문구, if/else(양쪽 끝이
  다른 경우) 들여쓰기, 반복(CPR 예시와 같은 판단→처리→되돌아가기 구조)의 ↻ 표시, 점검하기(빈칸 있음/
  없음 둘 다), 미리보기 실시간 갱신, 글로 저장(.txt), 새로고침 후 답 복원까지 확인했다. `npm run build`
  로 만든 `dist/` 에 `algorithm-core.js` 가 제대로 복사되는 것도 확인함.
- 실제 교실 사용은 아직 안 해 봤다. 수업 뒤 피드백으로 문제 상황·문구를 다듬을 것.
- 아직 안 한 것
  - [ ] GitHub 저장소 만들고 Actions 배포 (`.github/workflows/deploy.yml` 없음)
  - [ ] `hub/data.js` 3단원 `activities` 에 항목 추가 (지금은 `status: 'soon'`으로만 넣어 둠 — 배포 후 `url` 채우고 `'ready'`로)
  - [ ] `dist/` 빌드 결과를 실제 브라우저로 열어서 확인 (지금까지는 `npm start` 개발 서버로만 확인함)

---

# 여기부터는 고치기 전에 꼭 읽을 것

## 기호 내용 수정은 캔버스 위 입력창, 판단 분기는 confirm()

**2026-08-03 오후 변경.** 처음엔 기호 내용 수정도 `prompt()`로 만들었다가, 사용자가 "별도 입력창이
뜨지 않고 바로 입력할 수 있게 해 달라"고 요청해서 캔버스 위에 진짜 입력창(`<textarea class="fc-inline-editor">`)을
띄우는 방식으로 바꿨다. 구현은 `main.js`의 `openInlineEditor()`/`positionInlineEditor()`/
`repositionInlineEditor()`에 있다.

- **입력창은 SVG 밖, `document.body`에 `position: fixed`로 붙인다.** SVG는 `viewBox="0 0 1100 650"`
  고정 좌표를 쓰고 화면에는 CSS로 늘려 보이므로, `getBoundingClientRect().width / 1100` 비율로
  기호의 SVG 좌표를 화면 좌표로 바꿔 그 위치에 정확히 겹친다. **`position: fixed`라서 페이지가
  스크롤되면 위치가 어긋난다** — 그래서 `window`의 `scroll`(capture:true, `canvas-wrap`의 내부
  스크롤까지 잡으려고)과 `resize`에 `repositionInlineEditor()`를 걸어 뒀다.
- **편집 중인 기호는 SVG `<text>`를 그리지 않는다** (`buildNodeEl()` 의 `node.id !== editingNodeId` 분기).
  입력창이 바로 위에 겹쳐 있으므로 텍스트를 이중으로 그리면 안 된다.
- **커밋은 세 가지 경로**: `Enter`(줄바꿈 없이) → 저장, `Esc` → 취소(원래 값 유지), 포커스를 잃으면(blur,
  즉 다른 곳을 클릭) → 저장. `blur`가 프로그램적으로 두 번 불려도 안전하도록 `done` 플래그로 막았다.
  ⚠️ **자동화 브라우저(헤드리스 등)에서는 `document.hasFocus()`가 거짓이라 `.blur()`가 실제로
  이벤트를 안 낼 수 있다** — 실제 사람이 쓰는 브라우저에서는 정상 동작한다. 이 파일을 고친 뒤
  블러 커밋을 테스트할 때 이 점을 헷갈리지 말 것.
- **판단 기호의 예/아니요는 여전히 `confirm()`이다** (텍스트 입력이 아니라 둘 중 하나를 고르는 것이라
  대화상자가 더 낫다고 판단해 그대로 뒀다). 첫 번째 나가는 화살표에서만 물어보고
  (`확인=예, 취소=아니요`), 두 번째 화살표는 `decisionEdgeLabels()`로 남은 라벨을 자동으로 붙인다.
  파괴적 동작(전체 지우기·처음부터 다시)의 `confirm()`/`alert()`도 그대로 뒀다.

## 되돌리기(undo)·다시 실행(redo) — 2026-08-03 추가

`undoStack`/`redoStack`은 **`nodes`/`edges`의 전체 상태(JSON 깊은 복사)를 통째로 스냅샷**하는
가장 단순한 방식이다. 부분 diff 를 만들지 않는다 — 이 앱의 기호 개수(보통 10개 안쪽)를 생각하면
매번 통째로 복사해도 성능에 문제가 없고, 코드가 훨씬 단순해진다.

- **"놓기 + 놓자마자 입력한 이름"은 되돌리기 한 단계로 합친다.** `openInlineEditor(node, skipHistory)`의
  두 번째 인자가 `true`면(새로 놓은 기호에 자동으로 여는 경우) 내부에서 히스토리를 쌓지 않는다 —
  기호를 놓을 때 호출하는 `pushHistory()`가 이미 "그 기호가 없던 상태"를 기록해 뒀기 때문이다.
  그래서 되돌리기 한 번으로 방금 놓고 이름 붙인 기호가 통째로 사라진다.
  **반면 이미 있던 기호를 나중에 다시 눌러 이름을 고치는 것은 별도의 한 단계다**
  (`skipHistory` 없이 호출되고, 텍스트가 실제로 바뀌었을 때만 쌓는다 — 아무것도 안 바꾸고
  닫으면 되돌리기 단계가 늘어나지 않는다).
  → **이 둘을 헷갈려서 매번 히스토리를 쌓게 고치면, 기호 하나 만드는 데 되돌리기가 두 번 필요해진다.**
- **드래그(이동)도 한 단계다.** `pointerdown`에서 드래그 시작 전 스냅샷을 `dragState.beforeMove`에
  담아 두고, `pointerup`에서 실제로 움직였을 때(`moved===true`)만 그 스냅샷을 쌓는다. 움직이지
  않은 클릭(=편집 열기)에는 히스토리를 쌓지 않는다.
- **연결·삭제·전체 지우기**는 그 동작 바로 전에 `pushHistory()`를 부른다(스냅샷 인자 없이 —
  이때는 현재 상태를 그대로 찍으면 된다).
- **문제 정의 답(1단계)과 캔버스 크기는 되돌리기 대상이 아니다.** `snapshotState()`가
  `nodes`/`edges`만 찍는다. "도형 그리기"만 되돌린다는 요청 범위를 지킨 것이다.
- **히스토리는 새로고침하면 사라진다** (`localStorage`에 저장하지 않는다). 최대 50단계
  (`MAX_HISTORY`)까지만 쌓고 그 이상은 오래된 것부터 버린다.
- **`처음부터 다시`는 되돌릴 수 없다** — `localStorage`를 지우고 페이지를 새로고침하므로
  히스토리 자체가 사라진다. 확인 문구에 "되돌릴 수 없어요"를 그대로 남겨 둔 이유다. 반면
  `전체 지우기`(캔버스만)는 되돌릴 수 있어서 확인 문구에서 그 표현을 뺐다.

## 캔버스 크기 조절 — 2026-08-03 추가

`#canvas-resize-handle`(캔버스 오른쪽 아래 모서리)을 드래그하면 `VBOX_W`/`VBOX_H`(처음엔
1100×650)가 드래그한 픽셀만큼 바뀌고, `applyCanvasSize()`가 SVG의 `viewBox`·`width`·`height`
속성을 그 값으로 다시 쓴다. **줌(화면에 보이는 크기만 커짐)이 아니라 실제 그리는 공간 자체가
늘어나거나 줄어든다** — 그래서 캔버스를 키우면 기존 기호들은 그 자리 그대로 있고 여백만 늘어난다.

- **CSS에서 `#flowchart-svg`의 `width:100%`를 뺐다.** 원래는 항상 컨테이너 너비에 맞춰 늘어나는
  반응형이었는데, 그러면 `width`/`height` 속성을 바꿔도 화면에 보이는 크기가 안 바뀐다(항상
  꽉 채우니까). 지금은 `max-width:100%; height:auto;`만 남겨서, 화면이 충분히 넓으면
  속성값 그대로(1:1 픽셀) 보이고 좁은 화면에서만 비율을 유지하며 줄어든다.
- **최소/최대값**: `MIN_VBOX_W/H` = 700/400, `MAX_VBOX_W/H` = 2400/1800. 이 값들은
  `clamp()`로 강제되므로 아무리 세게 드래그해도 벗어나지 않는다.
- **좌표 변환(`toSvgPoint`)은 그대로 쓸 수 있다** — `VBOX_W`를 상수가 아니라 변수로 바꿔서,
  드래그 후에도 `rect.width / VBOX_W` 계산이 항상 최신 크기를 반영한다. 이 변수를 다시
  상수로 되돌리면 크기 조절이 깨진다.
- 크기도 `localStorage`(`canvasW`/`canvasH`)에 저장되어 새로고침해도 유지된다.

## 배치 모드가 sticky 이고, 드래그·편집이 배치 모드에서도 되는 이유

팔레트에서 기호를 한 번 고르면(`setMode('place:'+type)`) 캔버스를 여러 번 눌러도 계속 같은 기호가
놓인다. 같은 팔레트 버튼을 다시 눌러야 배치 모드가 꺼진다(`main.js` `shape-btn` 클릭 핸들러).
학생들이 순차 구조에서 처리 기호를 여러 개 연달아 놓는 경우가 많아서, 매번 팔레트를 다시 누르게 하면
불편하다고 판단해 이렇게 만들었다. **연결하기/삭제하기 모드도 같은 이유로 sticky다** (연속으로 여러 개
연결·삭제하는 경우가 많음).

**2026-08-03 오후 변경.** 처음엔 배치 모드일 때 드래그가 안 됐다(포인터 다운 핸들러가
`mode !== null` 이면 곧바로 return 했다) — 사용자가 "이미 놓은 기호가 배치 모드에서 드래그도,
편집도 안 된다"고 지적해서 고쳤다. 지금은 `pointerdown`이 `mode === 'connect' || mode === 'delete'`
일 때만 무시하고, 그 외(기본 모드·배치 모드 전부)에서는 **기존 기호를 누르면 드래그(움직이면)
또는 편집(안 움직이면 입력창 열기)**으로 처리한다. 빈 캔버스 클릭만 `click` 이벤트로 넘어가서
배치 모드일 때 새 기호를 놓는다. → **배치 모드에서 기존 기호를 클릭하는 처리를 `click` 리스너에
다시 넣지 말 것** — `pointerdown`/`pointerup`이 이미 처리하므로 중복된다.

## 3단계 알고리즘 설계 — 2026-08-03 저녁 추가

그래프를 따라가는 계산(`buildOutline`)과 화면을 그리는 코드(`renderAlgoOutline` 등, `main.js`)를
분리했다 — `flowchart-core.js`/`main.js` 를 나눈 것과 같은 이유(순수 로직은 node 로 시험 가능해야
한다). `algorithm-core.js` 는 `flowchart-core.js` 를 참조하지 않는다(독립적으로 동작, 중복은
감수했다) — 두 파일이 서로 의존하면 한쪽을 고칠 때 다른 쪽 시험이 깨질 위험이 커진다.

- **탭을 열 때마다 `buildOutline(nodes, edges)` 를 다시 부른다.** 결과를 캐시하지 않는다 — 학생이
  2단계로 돌아가 순서도를 고치고 다시 3단계로 오면 항상 최신 구조를 반영해야 하기 때문이다. 그 대신
  **학생이 쓴 답(`algoAnswers`)은 기호 `id` 로 저장**하므로, 순서도를 약간 고쳐도(기호를 옮기거나
  이름만 바꾸는 정도) 이미 쓴 답은 그대로 남는다. 기호를 지우면 그 답은 그냥 쓸 곳이 없어질 뿐 에러는
  나지 않는다.
- **3단계는 2단계가 `FlowchartCore.validate().ok === true` 일 때만 연다** (`renderAlgoOutline()` 맨
  앞에서 확인). 완성되지 않은 순서도를 억지로 번역하려 들면 `buildOutline` 이 이상한 결과를 낼 수
  있어서, 애초에 못 들어오게 막는 쪽을 골랐다.
- **들여쓰기 규칙이 헷갈리기 쉽다.** `buildOutline` 이 주는 `depth` 는 "판단 갈래로 몇 단 들어갔나"이고,
  가지 이름표(`kind:'branch'`, "그렇다면(예):"/"아니면(아니요):")와 그 안의 실제 내용이 **같은 depth
  값**을 갖는다(판단 기호 자체보다 1 크다). 그런데 화면에는 이름표가 내용보다 한 단 얕게 보여야
  자연스럽다("그렇다면:" 은 판단과 같은 줄, 그 안 내용만 들여쓰기). 그래서 `main.js` 의
  `algoRenderDepth(step)` 이 `kind==='branch'` 일 때만 `depth-1` 로 보정한다. **이 보정을 `main.js`
  가 아니라 `algorithm-core.js` 쪽으로 옮기려고 하지 말 것** — `algorithm-core.js` 의 `depth` 는
  "구조적으로 몇 단 깊은가"를 뜻하고(판단이 중첩되면 자연스럽게 커짐), 화면 들여쓰기 보정은 순수하게
  렌더링 관심사라서 `main.js` 에 두는 것이 맞다. `tests/run-algorithm-tests.js` 도 보정 전(raw depth)
  값을 검사한다.
- **`kind:'loop'`/`kind:'merge'` 는 학생이 입력할 칸이 없다.** 이미 지나온 기호로 되돌아가거나
  다른 갈래와 다시 만나는 지점이라는 것 자체가 그래프 구조로 결정되는 사실이라, 학생에게 문장을
  새로 쓰게 하면 "반복이다/아니다"를 스스로 판단하는 연습 대신 형식적인 타이핑만 남는다. 대신
  `algo-marker` 로 자동 표시만 하고, `AC.checkAnswers()` 도 `kind==='node'` 인 항목만 빈칸 검사
  대상으로 삼는다(→ 위 algorithm-core.js 계약 그대로).
- **미리보기(`#algo-preview`)와 PDF 보고서의 의사코드는 같은 함수(`buildPseudocodeText()`)를 쓴다.**
  화면 미리보기와 내보낸 결과의 줄바꿈·들여쓰기가 어긋나지 않도록 하나로 합쳐 뒀다.
  (2026-08-04에 `.txt` 내보내기를 없앤 뒤에도 이 함수는 미리보기·PDF 둘이 쓰므로 지우지 말 것.)
  **PDF 보고서의 의사코드도 같은 함수를 쓴다** — 세 곳의 내용이 절대 어긋나지 않는다.

## 설명문(`.lead`)은 문장마다 한 줄 — 2026-08-04

각 단계 맨 위 설명문은 **문장 하나가 한 줄**을 차지한다. `<p class="lead">` 안에서 문장마다
`<span class="lead-line">` 으로 감싸고 CSS에서 `display: block` 을 준다.

- **원래 `.lead { max-width: 900px }` 였는데 없앴다.** 본문 영역이 1144px 인데 글줄만 900px 로
  묶여 있어서, 오른쪽에 250px 가까이 빈 자리가 남고 문장이 어중간한 데서 끊겼다.
- **그러면 폭 제한만 없애면 되지 않나?** 안 된다. 1·2단계 설명문은 두 문장을 이어 붙이면
  약 1150px 로 **1144px 를 아주 살짝 넘겨서** 또 문장 중간에 끊긴다. 그래서 문장마다 줄을
  나누는 쪽으로 갔다 — 이러면 어느 화면에서도 문장이 중간에 끊기지 않는다.
- 가장 긴 문장이 **789px** 이라 창 너비가 **약 825px 이상이면 모두 한 줄**에 들어간다
  (교실 TV·전자칠판·노트북 모두 해당). 그보다 좁아지면 그 문장 안에서 자연스럽게 줄바꿈된다.
- 3단계 설명문은 원래 두 문장이었는데, 첫 문장이 길어서 「의사코드가 무엇인지」 설명하던
  괄호를 **따로 한 문장으로 떼어 세 줄**로 만들었다.
- → **문장을 길게 고치면 한 줄을 넘길 수 있다.** 설명문을 손볼 때는 브라우저에서
  `.lead-line` 의 `getClientRects().length` 가 1인지(=한 줄인지) 확인할 것.

## 학번·이름 — 2026-08-04 추가 (★ 저장하지 않는 것이 핵심)

1단계 맨 위에 **학번·이름** 칸이 있다(`#input-student-id`, `#input-student-name`).
PDF 보고서 머리말(`#print-student`)에 넣어 **교사가 누구 것인지 알 수 있게** 하려는 것이다.

- ⚠️ **`localStorage` 에 저장하지 않는다.** `save()`/`load()` 에 일부러 넣지 않았고,
  입력 핸들러도 `save()` 를 부르지 않는다(변수 `studentId`/`studentName` 에만 담긴다).
  **이유가 둘이다.**
  1. **교실 컴퓨터는 여러 학생이 돌려 쓴다.** 저장하면 다음 시간 학생이 앱을 열었을 때
     앞 학생의 이름이 그대로 남아 있다 — 이름이 남 화면에 뜨는 것 자체가 문제고,
     자기 이름인 줄 알고 그대로 제출하는 사고도 난다.
  2. **프로젝트 규칙**(루트 `CLAUDE.md`)이 "학생 개인정보를 수집·저장하는 기능은 만들지 않는다"다.
     이름을 **받아서 즉시 PDF에 찍고 버리는 것**은 이 규칙과 어긋나지 않지만,
     디스크에 남기면 어긋난다.
  → **"새로 고치면 이름이 사라지는데 불편하니 저장하자"고 고치지 말 것.** 이름 다시 치는
     2초와, 남의 이름이 남는 사고를 견주면 지금 쪽이 맞다. 문제 정의·순서도(공부한 내용)는
     그대로 저장되므로 새로 고쳐도 잃는 것은 이름뿐이다.
  → 화면에도 이 성질을 적어 뒀다(`#student-block` 의 안내문, 바닥글). 동작을 바꾸면 그 문구도 고쳐야 한다.
- **1단계 통과 조건(게이트)에는 넣지 않았다.** 게이트는 *학습 내용*(문제 상황·문제·초기/목표 상태)을
  다 썼는지 보는 것이라, 이름을 안 썼다고 순서도를 못 그리게 하는 것은 과하다.
  대신 **PDF를 만들 때 둘 다 비어 있으면 `confirm()` 으로 한 번 물어본다** — 「취소」를 누르면
  1단계로 데려가 학번 칸에 커서를 놓는다. 이름 없이 뽑고 싶을 수도 있으니 막지는 않는다.
- 둘 다 비어 있으면 PDF 머리말 줄 자체를 `display:none` 으로 감춘다(빈 줄이 남지 않게).
  하나만 썼으면 쓴 것만 나온다.

## 1단계를 다 써야 넘어간다 — 2026-08-03 밤 추가

교과서 흐름(문제 정의 → 순서도 → 알고리즘)을 건너뛰지 못하게, **1단계의 네 항목**
(문제 상황 / 해결해야 할 문제 / 초기 상태 / 목표 상태)이 다 채워질 때까지 2·3단계로 이동하지 않는다.

- **막는 곳은 `goToTab()` 한 군데다.** 이동 경로가 넷(탭 버튼 3개 + 「2단계로」 버튼 + 「3단계로」
  버튼 + 3단계의 「2단계로 돌아가기」)이라, 각 버튼마다 검사를 붙이면 하나 빠뜨리기 쉽다. 그래서
  실제 화면을 바꾸는 `showTab(name)` 과 **검사를 거치는 유일한 입구** `goToTab(name)` 을 나눠 두고,
  모든 이동은 `goToTab()` 만 부른다. → **새 이동 버튼을 추가하면 반드시 `goToTab()` 을 부를 것.**
  `showTab()` 을 직접 부르면 검사를 건너뛴다.
- **막을 때 1단계 화면으로 되돌린다.** 3단계에 있다가 막히면 그 자리에 머무르는 것이 아니라
  `showTab('define')` 로 1단계를 열고 경고를 그린다 — 그러지 않으면 학생이 보이지 않는 화면에
  그려진 경고문을 못 보고 "버튼이 고장났다"고 느낀다.
- **`문제 상황`은 두 가지로 비어 있을 수 있다.** ① 카드를 아예 안 골랐을 때(`scenarioId` 가 null)
  ② 「직접 입력하기」를 골랐는데 아무것도 안 썼을 때. 카드를 골랐으면 `situationText` 에 그 카드의
  글이 들어가므로 자동으로 채워진다. 카드를 안 고른 경우에는 **`#scenario-cards` 에 빨간 점선**을,
  직접 입력이 빈 경우에는 **`#situation-block` 을** 표시한다(둘을 구분해서 어디를 봐야 할지 알려 준다).
- **공백만 쓴 것도 안 쓴 것으로 본다** (`String(...).trim()`). 학생이 스페이스만 눌러 통과하는 것을 막는다.
- **처음 화면에서는 빨간 표시를 하지 않는다.** 아직 아무것도 시도하지 않은 학생에게 빈 칸이 전부
  빨갛게 보이면 위협적이다. **넘어가려고 눌렀을 때 처음 표시**되고, 그 뒤부터는
  `refreshDefineFeedbackIfShown()` 이 입력·카드 선택마다 목록을 다시 계산해 **채우는 즉시 줄어든다.**
  이 함수는 **경고가 이미 떠 있을 때만** 동작한다(`#define-feedback` 이 `hidden` 이면 그냥 반환) —
  그래서 첫 방문에는 조용하다. 새로고침하면 다시 조용한 상태로 돌아간다.
- **2·3단계에 이미 들어와 있는 학생을 강제로 쫓아내지는 않는다.** 검사는 *이동할 때만* 한다.
  1단계로 돌아가 칸을 지워도 그 순간 튕겨 나가지 않고, 다음에 이동할 때 막힌다.

## PDF로 저장 — 2026-08-03 밤 추가

**외부 PDF 라이브러리(jsPDF, html2pdf 등)를 쓰지 않는다.** 브라우저의 인쇄 기능
(`window.print()` + `@media print` CSS)으로 만든다. **일부러 이렇게 했다.**

1. **의존성 0개를 지킨다.** 이 앱은 `index.html` 을 더블클릭해도 열려야 하고(CDN·`fetch`·ES 모듈
   전부 안 씀), 교실 인터넷이 끊겨도 동작해야 한다. PDF 라이브러리를 넣으면 번들이 수백 KB 커지고
   더블클릭 실행도 깨진다.
2. **한글이 깨지지 않는다.** jsPDF 계열은 한글을 쓰려면 폰트 파일(수 MB)을 따로 끼워 넣어야 하고
   그러지 않으면 글자가 사라지거나 □ 로 나온다. 브라우저 인쇄는 시스템 폰트를 그대로 쓴다.
3. **SVG 순서도가 벡터 그대로 들어간다.** 캔버스를 PNG 로 굽지 않고 SVG 를 그대로 복사해 넣으므로
   확대해도 안 깨진다.

동작 방식:

- `index.html` 맨 아래 `<div id="print-report" class="print-only">` 가 있다. **화면에서는 항상
  `display:none`** 이고, 버튼을 누르면 `main.js` 가 그 안(제목·문제 상황·정의 3항목·순서도 SVG
  복사본·의사코드)을 채운 다음 `window.print()` 를 부른다.
- `@media print` 안의 **`body > *:not(#print-report) { display: none !important; }`** 가 나머지
  화면(머리말·탭·본문·바닥글)을 인쇄에서 숨긴다. → **`#print-report` 는 반드시 `<body>` 의
  직계 자식이어야 한다.** `main` 안으로 옮기면 이 선택자가 안 먹어서 화면 전체가 같이 인쇄된다.
- 순서도는 `svg.cloneNode(true)` 로 복사하고 **복사본의 `id` 를 지운다**(`#flowchart-svg` 가 문서에
  두 개가 되면 안 된다). 인쇄 배경이 흰색이도록 흰 `<rect>` 를 맨 앞에 깔아 준다 — PNG 내보내기와
  같은 방식이다.
- 학생 안내: 버튼 아래 `#algo-pdf-note` 에 "프린터 항목에서 'PDF로 저장'을 고르세요"라고 적어 뒀다.
  **브라우저가 저장 위치·파일명을 정하므로 앱이 파일명을 지정할 수 없다** (`.txt` 내보내기와 다른 점).
- 순서도가 없으면(`algoSteps` 가 빈 배열) `alert()` 로 막고 인쇄 대화상자를 열지 않는다.

## 좌표계

SVG는 항상 `viewBox="0 0 1100 650"` 내부 좌표를 쓴다. `toSvgPoint()` 가 `getBoundingClientRect()` 의
너비 비율(`1100 / rect.width`)로 화면 좌표 → SVG 좌표를 바꾼다. **노드의 `x, y`는 중심점이다**
(모서리가 아니다) — `boundaryPoint()`, 드래그 클램프(`clamp(..., node.w/2, VBOX_W - node.w/2)`),
렌더링(`translate(x - w/2, y - h/2)`)이 전부 이 가정 위에 있다. 이 가정을 바꾸려면 세 곳을 다 고쳐야 한다.

## 검사 규칙을 고칠 때

`FlowchartCore.validate()`의 규칙 문구·순서를 고치면 `tests/run-tests.js` 의 25개 이상 테스트 중
`hasIssue()` 로 문자열을 찾는 부분이 깨질 수 있다. **메시지 문구를 바꾸면 테스트의 검색 문자열도 같이
고칠 것.** `npm test` 로 항상 확인.

## 개인정보

학생이 입력한 문제 정의·순서도는 **이 컴퓨터의 브라우저 `localStorage`에만** 있다(`algorithm-flowchart:v1`).
서버 전송 없음, 계정 없음. 이 성질을 바꾸지 말 것 (프로젝트 규칙: 학생 개인정보 수집·저장 금지).

## 파일

| 파일 | 하는 일 |
|:---|:---|
| `index.html` | 화면 뼈대. 1단계(문제 정의)·2단계(순서도 그리기)·3단계(알고리즘 설계) 세 탭 + 맨 아래 인쇄 전용 보고서(`#print-report`, `<body>` 직계 자식이어야 함) |
| `style.css` | 3단원 색(보라 `#8b5cf6`) 기준. 본문 20px, 제목 34px. 외부 글꼴 없음. `.lead-line` 로 설명문을 문장마다 한 줄씩(아래 절 참고). 맨 아래 `@media print` 로 PDF 보고서 서식 |
| `flowchart-core.js` | 기호(node)·화살표(edge) 데이터 구조와 `validate()`. **화면 코드 없음** → node로 시험 가능 |
| `algorithm-core.js` | 순서도를 시작 기호부터 따라가며 순차/선택(if-else)/반복(loop)/합쳐짐(merge) 구조를 찾는 `buildOutline()`, 빈칸 검사 `checkAnswers()`. **화면 코드 없음, `flowchart-core.js`에도 의존하지 않음** → node로 단독 시험 가능 |
| `scenarios.js` | 문제 상황 카드 4개 + 직접 입력하기 (문제 상황·힌트 문구는 여기만 고치면 됨) |
| `main.js` | SVG 그리기, 배치·연결·삭제·드래그, 되돌리기/다시 실행, 캔버스 크기 조절, 3단계 의사코드 칸 렌더링·미리보기·점검하기, PNG·PDF(인쇄) 내보내기, `localStorage` 저장/복원 (**학번·이름은 저장 제외**) |
| `tests/run-tests.js` | 단위 시험 52개 (`npm test`, `flowchart-core.js` 대상) |
| `tests/run-algorithm-tests.js` | 단위 시험 41개 (`npm test`, `algorithm-core.js` 대상) |
| `vite.config.mjs` | 포트 8086, `base: './'`, 일반 스크립트 복사 플러그인(`flowchart-core.js`/`algorithm-core.js`/`scenarios.js`/`main.js`) |
